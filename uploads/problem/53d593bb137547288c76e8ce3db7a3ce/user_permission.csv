from rest_framework.response import Response
from rest_framework import status
from account.models import User
from classes.models import Class, Class_user

def user_perm(username, privilege):
    user = User.objects.get(username=username)
    pri = user.privilege
    if pri >= privilege:
        return True
    else:
        return False

def class_user_check(username, class_id):
    count = Class_user.objects.filter(username=username).filter(class_id=class_id).count()
    if count != 0:
        return True
    else:
        return Response(status=status.HTTP_400_BAD_REQUEST)

# def class_perm(username, class_id, privilege):

#     if self.paginator is None:
#         return None
#     return self.paginator.paginate_queryset(queryset,
#             self.request, view=self)

